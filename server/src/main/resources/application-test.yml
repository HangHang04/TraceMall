spring:
  config:
    activate:
      on-profile: test
  datasource:
    url: jdbc:h2:mem:tracemall_test;MODE=MySQL;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
    username: sa
    password:
    driver-class-name: org.h2.Driver
  flyway:
    enabled: false
  data:
    redis:
      host: localhost
      port: 6379

logging:
  level:
    root: warn
